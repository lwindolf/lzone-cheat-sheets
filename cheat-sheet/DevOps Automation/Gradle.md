## Commands

      gradle build
      gradle tasks          # List build targets
      gradle cleanup

## Using the wrapper

Generate the gradlew wrapper for your project

      gradle wrapper

Run the wrapper

      gradlew <gradle options>
