### Command Overview

    bundle      # installs
    bundle install
    bundle install --path <some other path>

    bundle update
    bundle outdated

    bundle list
    bundle check
    bundle console

    bundle exec <command>

    bundle clean
    bundle clean --force
